version: '3.8'

services:
  gift-stats:
    build: .
    container_name: telegram-gifts-stats
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
